// var chalk = require('chalk'); // This is ES5 syntax
import chalk from 'chalk';    // This is ES6 syntax. With babel, this should transpile to ES5.

console.log(chalk.green('Starting app in dev mode...'));
